# Support Check Flow Tests

These tests exercise matcher-rule-change infrastructure. They are separate
from `app/tests/`, which is used for local batch-review work and is not the
right place for committed support-check coverage.
