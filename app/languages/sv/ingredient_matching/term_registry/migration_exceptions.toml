# Temporary bridge for new legacy-only terms that must land before their source
# family has registry coverage. R4 treats every row as a narrow exception, not
# as a second source of truth.
#
# [[exceptions]]
# id = "sv-se.example.until_r1"
# owner = "codex"
# language = "sv"
# market = "SE"
# source_family = "match_bridge"
# canonical = "example"
# variant = "example variant"
# layer_role = "bridge_positive"
# reason = "Temporary legacy-only bridge until registry coverage handles this source family."
# created_at = "2026-05-09"
# expires_when = "Remove when registry coverage includes this exact key."
# expires_on = "2026-06-30"
